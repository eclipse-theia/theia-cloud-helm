# Session Deployments are generated by the operator and use this ServiceAccount for all supported cloud providers.
# Keep this name synchronized with the operator deployment templates.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: theia-cloud-sessions
  namespace: {{ .Release.Namespace }}
